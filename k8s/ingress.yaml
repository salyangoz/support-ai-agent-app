apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: support-ai-app
  namespace: support-ai
  annotations:
    cert-manager.io/cluster-issuer: letsencrypt-prod
    nginx.ingress.kubernetes.io/proxy-body-size: "10m"
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - support-app.yengec.co
      secretName: support-ai-app-tls
  rules:
    - host: support-app.yengec.co
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: support-ai-app
                port:
                  number: 80
